const { parseEther } = require("ethers/lib/utils");

const PRICE = parseEther("0.002"); // TODO: change price to 4.44
const PRICE_MARRIAGE = parseEther("0.003"); // TODO: change price to 4.55

module.exports = { PRICE, PRICE_MARRIAGE };
