const { parseEther } = require("ethers/lib/utils");

const PRICE = parseEther("0.002"); // TODO: change price to 3.33
const PRICE_WEDDING = parseEther("0.003"); // TODO: change price to 3.33

module.exports = { PRICE, PRICE_WEDDING };
